active = true;

// I want to disable and enable my demo object
// when making a textbox, so I pass in this function
// to reactivate when the dialogue ends
activate = function() {
	active = true;
}

a = 1;